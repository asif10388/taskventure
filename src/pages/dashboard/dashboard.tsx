import { useEffect, useState } from "react";
import { useNavigate } from "react-router-dom";
import { supabase } from "../../utils/supabase";

const Dashboard = () => {
  return <div>Welcome, {"ID NOT FOUND" || "ID not found"}</div>;
};

export default Dashboard;
